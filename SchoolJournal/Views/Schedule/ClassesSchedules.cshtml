@model List<Class>
@{
    Layout = "_MainLayout";
    ViewBag.Title = "Список розкладів";
}
<h4 class="text-center mb-4 mt-3">Список розкладів для всіх класів</h4>
<div class="row justify-content-center">
    <div class="col-lg-7">
        <table class="table">
        @foreach(Class c in Model)
        {
            <tr>
                <td class="text-center">Розклад для @c.Title</td>
                <td class="text-center"><a asp-action="AdminSchedule" asp-route-fkClass="@c.Id" class="btn btn-sm" 
                    style="background-color: #2F3E46; color: #CAD2C5;">Переглянути</a></td>
            </tr>        
        }
        </table>
    </div>
</div>
